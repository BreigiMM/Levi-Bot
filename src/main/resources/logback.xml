<configuration>
    <timestamp key="runTimestamp" datePattern="yyyy-MM-dd_HH-mm-ss" />

    <!-- Console Appender -->
    <appender name="CONSOLE" class="ch.qos.logback.core.ConsoleAppender">
        <encoder>
            <pattern>%d{yyyy-MM-dd HH:mm:ss} [%thread] %-5level %logger{36} - %msg%n</pattern>
        </encoder>
    </appender>

    <!-- File Appender -->
    <appender name="FILE" class="ch.qos.logback.core.FileAppender">
        <file>logs/levi-${runTimestamp}.log</file> <!-- Fixed filename per run -->
        <append>false</append> <!-- Start fresh every time -->
        <encoder>
            <pattern>%d{yyyy-MM-dd HH:mm:ss} [%thread] %-5level %logger{36} - %msg%n</pattern>
        </encoder>
    </appender>

    <root level="debug">
        <!-- <appender-ref ref="CONSOLE" /> --> <!-- Uncomment to enable console output -->
        <appender-ref ref="FILE" />
    </root>
</configuration>
